# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['inference_server.py'],
    pathex=[],
    binaries=[],
    datas=[('../handmodel/cnn_model_bothhands.keras', 'handmodel'), ('../handmodel/dynamic_gesture_model3.h5', 'handmodel')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='inference_server',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
